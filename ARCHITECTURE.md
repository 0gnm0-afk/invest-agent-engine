# 설계 문서 (ARCHITECTURE)

## 목표

1. 매매일지를 기록하고 복기해주는 시스템
2. 종목을 지정하면 여러 관점(거시경제, 투자 거장 방법론)에서 분석해주는 시스템
3. 최상위 모델을 계속 갈아끼울 수 있는(모델 비종속) 구조
4. **책을 읽으며 시스템이 자라는 구조**: 문헌 발췌를 올리면 AI가 방법론 노트·분석규격·
   데이터 요구사항으로 소화한다

## 핵심 설계 원칙

### 원칙 1 — 자산은 하네스가 아니라 vault다

가치가 쌓이는 것은 오케스트레이션 코드가 아니라 **방법론 노트 + 매매일지 데이터 +
종목 분석 아카이브 + 문헌 소화 기록**이다. 하네스(Claude Code, Codex CLI, 자체
오케스트레이터)는 교체 가능한 소모품으로 취급한다.

### 원칙 2 — 방법론은 코드가 아니라 데이터. 단, "페르소나 극장" 금지

에이전트의 지능은 방법론 노트(markdown)에서 온다:

- 노트를 산문 통째로 프롬프트에 붓지 않는다. **체크리스트·판정 기준·금지 조건**으로 구조화.
- 분석 출력 계약 고정: `기준 → 확인된 근거 → 미확인 정보 → 반대 논거 → 잠정 판단`
- 시스템 프롬프트에는 공통 규칙만: 근거 없는 추측 금지, 모르면 보류 등.

### 원칙 3 — 모델 비종속 = 모든 것을 텍스트로. 단, 데이터는 스키마를 가진다

사람이 읽는 자산은 마크다운, 거래·가격·재무 데이터는 **YAML frontmatter 고정 스키마**
(`market`/`currency`/`ticker` 포함 — 시장 확장 여지). 문헌 입력 계약도 "페이지 참조가
있는 발췌 텍스트"로 고정해 입력 도구에 비종속.

### 원칙 4 — 진입 기록은 불변 (사후 합리화 방지)

매매일지의 진입 당시 기록(논지·시나리오·감정)은 수정하지 않는다. 결과·복기는 별도
섹션에 추가만 한다.

### 원칙 5 — 공개 엔진 / 비공개 인스턴스 분리

- **공개 repo (엔진, 이 저장소)**: 어댑터·리포트 생성 코드, 스키마, 빈 템플릿, 범용
  에이전트 정의, 합성 예시, 아키텍처 문서.
- **비공개 (개인 인스턴스)**: 매매일지, 보유종목·포지션, 실제 분석·복기, API 키,
  데이터 캐시, 방법론 노트 내용, 문헌 원문·발췌 전부(저작권).
- `.gitignore`가 아니라 repo 물리 분리로 유출을 구조적으로 차단.

### 원칙 6 — 문헌은 원천자료, 노트는 파생 해석 (계보 보존)

발췌본이 원천자료이고 AI가 만든 방법론 노트는 파생물이다. 모든 추출 항목에 **귀속
태그**를 남긴다: `저자 해설` / `거장 견해의 재서술` / `거장 직접 인용` / `AI 해석·적용 제안`.
AI는 canonical 노트를 직접 덮어쓰지 않고 **검수대기본 + 변경 diff + 확인 질문**까지만 —
승인분만 반영된다.

## 3계층 구조

```
┌─────────────────────────────────────────────┐
│ 모델 계층: Claude / GPT / …  ← 설정으로 교체       │
├─────────────────────────────────────────────┤
│ 오케스트레이션 계층: 서브에이전트 실행·종합            │
│  1단계: CLI 에이전트의 서브에이전트로 시작            │
│  3단계: 필요해지면 Python + LiteLLM 자체 하네스     │
├─────────────────────────────────────────────┤
│ 지식 계층: Obsidian vault (진실의 원천)            │
└─────────────────────────────────────────────┘
```

### vault 구조 (비공개 인스턴스)

```
vault/
├── 00_인덱스/HOME.md
├── 10_방법론/
│   ├── 거장/            ← 해석 렌즈 (거장 1명 = 노트 1개)
│   ├── 기법/            ← 재무비율, Reverse DCF, 밸류에이션, 스크리닝 등
│   └── 거시_자산배분/    ← 경제지표, 경기 사이클, 자산군, 포트폴리오 구성
├── 15_분석규격/          ← 종목분석 출력 항목·체크리스트·필요 데이터 (독서가 자라게 함)
├── 20_매매일지/
│   ├── 계좌목록.md       ← 원계좌번호 없이 별칭·계좌유형만 관리
│   ├── 원본/             ← 증권사 거래내역 원본 (비공개, 수정 금지)
│   ├── 일지/            ← 진입 기록 (불변) + 사후 기록 섹션
│   └── 복기/
├── 30_종목/<종목명>/
├── 40_거시/             ← 매크로 브리핑 아카이브
├── 90_에이전트/          ← 서브에이전트 정의(입출력 계약)
└── 95_문헌/
    ├── 수독인덱스.md     ← 문헌·장·상태(읽기전/발췌됨/추출됨/반영됨)
    ├── 발췌/            ← 페이지 참조 보존 발췌본 (원천)
    ├── 추출/            ← 주장 단위 분해 + 귀속 태그
    ├── 검수대기/         ← 노트 반영 전 diff·확인 질문
    └── 반영기록/         ← 언제 어떤 노트에 무엇이 반영됐는지
```

## 독서 소화 파이프라인 (시스템의 성장 엔진)

```
책 읽기
  → 발췌·메모 (페이지 참조 유지 — 도구 무관, 텍스트면 무엇이든)
  → 95_문헌/발췌/ 저장 (문헌ID·장·읽은 날 메타데이터)
  → /독서: 주장 단위 추출 + 귀속 태그 + 라우팅 제안(어느 노트·분석규격·데이터 요구사항으로 갈지)
          + 기존 노트와 중복·충돌 검사 → 검수대기본 + 확인 질문 생성 (canonical 노트 수정 금지)
  → 사용자 검수·승인
  → /반영: 승인분만 대상 노트에 병합 + 반영기록 남김 → 노트 상태 draft → shadow
  → (계산식·스크리너는 예제 테스트 통과 후에만 active)
```

**"책 1권 = 에이전트 1개"가 아니라 "책 1장 = 여러 자산에 대한 변경 제안"이다.**
예: 밸류에이션 챕터 → 기법 노트(계산 개념) + 분석규격(내재 성장률 섹션) + 데이터
요구사항(가격·주식수·현금흐름) + (데이터 단계에서) 계산 코드.

### 품질 함정 체크리스트 (추출 시 검사)

- 종목명·인명·숫자·부정 표현의 오독
- 문헌 속 예시·일화를 일반 원칙으로 오인
- **저자(해설자)의 해설을 거장 본인의 견해로 귀속**
- 출간 시점의 시장 설명을 영구 투자 규칙으로 변환
- 도구 사용법을 일반 투자 방법론으로 오인
- 읽지 않은 장을 AI가 상식으로 임의 보충
- 새 문헌이 기존 노트를 조용히 덮어쓰기

## 방법론 렌즈 시스템

거장 1명마다 에이전트 파일을 늘리지 않는다. 공통 `방법론 평가기`가 선택된 노트를 읽어
그 관점의 분석을 생성한다. 노트 추가 = 렌즈 추가.

- **노트 상태 관문**: `draft`(정리 중/골격) → `shadow`(참고 의견으로만 표시) →
  `active`(출처·체크리스트·적용 범위·반례 완비)
- **노트 메타데이터**: 역할(종목선정/타이밍/리스크/자산배분), 적용 대상, 투자기간, 필요 데이터
  — 자산배분형 렌즈와 추세매매형 렌즈는 성격이 다르므로 관련 렌즈만 호출
- **첫 렌즈**: 클라만(정성 — 안전마진·하방위험, 억지 점수화 금지) + 그린블라트(계량 —
  마법공식의 한국 시장 적용)

## 매매일지

- **템플릿**: 거래 사실(계좌별칭·시장·종목·시각·수량·가격·수수료) / 데이터 기준 시각·출처 /
  투자 논지 1~3문장 / 예상 보유기간·촉매 / 기본·낙관·비관 시나리오 / **논지 무효화 조건** /
  포지션 크기 산출 근거 / 반대 논거·미확인 정보 / 확신도·감정·충동 여부 /
  사후 기록(별도 섹션: 결과, 계획 대비 이탈, 재발 방지 규칙)
- **기록 방식**: AI 인터뷰형 — 사용자는 큐만 던지고 `/일지`가 누락 항목을 질문해 구조화·저장
- **다계좌 원칙**: 원계좌번호는 일지에 쓰지 않고 `account_alias`만 기록. 증권사 원본은
  `20_매매일지/원본/`에 수정 없이 보관.

## 데이터 파이프라인

성숙한 오픈소스 위에 얇은 레이어만: **OpenDartReader(DART 공시) + FinanceDataReader(시세)**
로 시작, pykrx(수급)·dart-fss(XBRL)는 필요 확인 후. 실시간 현재가·호가·체결·캔들·환율은
증권사 Open API 조회 어댑터로 보강 — **조회 전용, 주문 API는 설계상 사용하지 않는다**.
각 소스는 `DataProvider` 인터페이스 뒤에 감추고, 모든 결과에
`source, fetched_at, as_of, adjusted, unit, currency, consolidated, report_id, error` 메타필드.

함정: 수정주가/단위·통화 불일치 결합, 종목코드↔고유번호 혼동, 연결/별도·누적/당분기 혼동,
정정공시 덮어쓰기, 빈 결과≠0, 캐시 공개 커밋 금지.

## 단계별 로드맵

- **0단계** — vault 뼈대 + 문헌 수신 준비 + 첫 일지 1건 (스키마·템플릿·리허설·공개 repo)
- **1단계** — CLI 에이전트 MVP: `/독서` `/반영` `/일지` `/복기` `/종목분석` + 공통
  방법론 평가기, 첫 두 렌즈(클라만·그린블라트) 연결, 라우터 + 종합기
- **2단계** — 데이터 파이프라인: DataProvider 계약 + DART·시세 어댑터 + 결합 가치평가
  리포트(ROIC·이익수익률) + 오류·정정공시 테스트
- **3단계** — 자체 하네스 (필요할 때만) / **4단계** — 자동화 (아침 브리핑, 변동 알림)

## 주의사항

- 시세·뉴스는 반드시 실시간 소스에서, 리포트에 데이터 기준 시점 명기.
- 에이전트 출력은 판단 보조 자료. 최종 판단·책임은 본인.
- 시스템 품질의 상한 = 방법론 노트 품질. `shadow` 렌즈의 의견을 과신하지 않는다.
- 문헌 원문·장문 발췌는 저작권상 비공개 — 공개 repo에는 합성 예시만.
