# 공개 패키지 경계

공개 대상은 이 폴더의 소스·테스트·합성 예시·설명서 및 `publication-manifest.json`에 고정한 파일입니다.
상위 작업 폴더, 원본 개발 저장소, 검사 작업 기록은 공개하지 않습니다.
`.git` 이력, 인증 도구, 실제 계좌 입력, 개인 리포트, DB, 백업, 환경설정은 포함하지 않습니다.

```sh
python scripts/check_publication.py --root .
```

검사기는 전체 파일 목록과 SHA-256을 매니페스트와 비교하고 개인정보·키 패턴을 검사합니다.
추가 파일·변경 파일·심볼릭 링크를 거부하며 실패 로그에 파일명이나 탐지 원문을 출력하지 않습니다.
파이썬 캐시와 가상환경도 공개 패키지 검사에서는 추가 파일로 간주합니다.
최종 게시 검사는 개발 폴더 대신 캐시와 환경을 제외한 깨끗한 사본에서 실행합니다.

이 검사는 스스로의 정당성을 보증하는 보안 경계가 아닙니다. 검사기와 매니페스트를 함께 바꾸면
내용이 달라질 수 있으므로, 검토된 별도 사본/해시를 신뢰 기준으로 보관해야 합니다.
개별 소유자의 실명 변형 검사는 비공개 준비 도구에서 별도로 수행합니다.

## 게시 경로

이 소스 사본의 상위 Git 저장소를 공개 원격에 연결하지 않습니다.
게시 준비 도구로 만든 독립 `upload-repo`와 파일 전용 ZIP을 사용합니다.
ZIP에는 `.git`, 검사 기록, 개인 키, 실제 입력/출력이 없습니다.

독립 저장소의 커밋 검사는 다음 명령으로 수행합니다.

```sh
python -B scripts/check_git_publication.py --repo .
```

커밋 검사는 전체 도달 가능한 이력의 작성자·커미터·메시지·파일경로·내용을 확인하고,
새 커밋마다 파일 목록과 해시를 매니페스트와 대조합니다. 최초 공개 기준 커밋은
매니페스트 도입 이전의 검토된 커밋으로 명시되어 있습니다. 파일명/메시지 원문을 로그에 출력하지 않습니다.
GitHub의 기존 `public-privacy-check` 검사 이름은 유지합니다. 정기 실행이나 배포는 없습니다.

저장소 내부의 CI·검사기·매니페스트는 함께 수정될 수 있으므로 절대적인 신뢰 경계가 아닙니다.
이번 게시 준비의 로컬 `pre-push` 훅은 검토 후 고정한 정확한 커밋, 원격 주소와 main 대상만 허용합니다.
파일을 고친 뒤 새 커밋을 만들면 차단됩니다. 검토 없이 훅을 우회하거나 기준 해시를 바꾸지 않습니다.
향후 다른 컴퓨터에서 clone하면 로컬 훅은 복제되지 않으므로 별도의 게시 전 검토가 필요합니다.
GitHub 검사 성공은 업로드 후의 탐지이며 이미 올라간 비밀을 숨기거나 회수하지 않습니다.

실제 계좌 입력·리포트는 키 기반 별명을 쓰더라도 항상 비공개입니다.
키 생성/유지와 기존 별명 전환은 TRADING.md의 개인 계좌 별명 키 절을 따릅니다.

## 보존한 과거 개발 기록

2026-07-14/2026-09-13의 두 커밋은 소유자가 실명 공개를 수용하고 원형 보존을 선택한
매니페스트 도입 이전의 역사적 기록입니다. 해당 정확한 커밋에만 매니페스트 예외를 적용합니다.
기존 공개 AI 공동작성자 주소 하나는 그 역사적 커밋에서만 허용하며, 다른 이메일과 키 검사는 유지합니다.
현재 파일과 이후 커밋에는 계속 매니페스트 검사를 적용합니다. 이 예외는 실제 계좌·개인 인증정보의
공개를 허용하지 않습니다. 과거 커밋을 되살리기 위해 전체 검사를 끄지 않습니다.
