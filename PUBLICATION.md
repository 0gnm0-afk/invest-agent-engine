# 공개 전 확인

현재 공개 대상은 문서·스키마·빈 템플릿·합성 예시와 게시 검사 도구다.
실제 계좌, 보유내역, vault, 리포트, 원문, API 자격증명은 별도 비공개 저장소에 둔다.

1. 새 파일이 필요하면 `scripts/check_publication.py`의 명시적 허용 목록을 검토한다.
2. 모든 공개 파일을 직접 읽는다. LICENSE와 README, 예시, 한글/영문 성명,
   개인 이메일과 경로를 포함해 확인한다. 개인 식별자 목록은 공개 저장소에 넣지 않는다.
3. 커밋 후 `python scripts/check_publication.py`를 실행한다. 현재 파일뿐 아니라
   도달 가능한 전체 이력의 파일과 작성자도 검사한다.
4. 별도 비공개 검사로 본인의 알려진 한글/영문 이름 변형과 개인 식별자를 확인한다.
   공개 검사기는 임의의 문장 속 모든 개인정보를 판별하지 못한다.
5. 원격의 실제 커밋·파일 내용과 로그아웃 상태의 공개 화면을 확인한다.

실명은 LICENSE에 쓰지 않고 공개 별명을 사용한다. 이름을 고친 후속 커밋만
추가해도 이전 이력에는 실명이 남을 수 있다. 과거 유출 시 현재 파일 수정과
과거 객체의 공개 접근 제거를 따로 확인한다.

`.gitignore`는 예방 장치이며 이미 추적 중인 파일이나 강제 추가를 차단하지 않는다.
허용 목록 검사와 직접 검토를 함께 사용한다. CI 실패 로그는 발견한 값 원문을 출력하지 않는다.
원본 백업의 Git 이력을 공개 저장소에 다시 푸시하지 않는다.
